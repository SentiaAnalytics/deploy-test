install:
  - echo "install"
  - docker build -t cullophid/hello-world .

script:
  - echo "script"

after_success:
  - echo "after success"

after_failiure:
  - echo "after fail"

after_script:
  - echo "after script"
